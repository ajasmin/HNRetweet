(ns HNRetweet.creds)

; fill out secrets and copy to creds.clj

(def twitter-consumer (oauth/make-consumer
                        "consumer token"
                        "consumer token secret"
                        "http://twitter.com/oauth/request_token"
                        "http://twitter.com/oauth/access_token"
                        "http://twitter.com/oauth/authorize"
                        :hmac-sha1))

(def twitter-token "my-token")
(def twitter-secret "my-secret")
